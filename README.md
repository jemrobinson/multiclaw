# Install NemoClaw
Run `./install.sh`

# Add a sandbox
Run `./sandbox.sh $SANDBOX_NAME`

# Update a sandbox
Run `nemoclaw $SANDBOX_NAME rebuild`

# Connect to sandbox TUI
Run

```bash
nemoclaw $SANDBOX_NAME connect
openclaw tui
```